/* eslint no-unused-vars: "off" */

export enum JointEnum {
  knee = 'knee',
}

export enum MovementTypeEnum {
  rom = 'rom',
  dynamic = 'dynamic',
  both = 'both',
}

export enum SideEnum {
  left = 'left',
  right = 'right'
}
